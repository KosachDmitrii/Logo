OPENAI_API_KEY=
RECRAFT_API_KEY=
GEMINI_API_KEY=

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=logo-files

# Temporary: Local Studio identity on Railway (until real login).
# Set to 1 on Railway Variables.
ALLOW_LOCAL_STUDIO=

# Local UI → Railway API (recommended). Restart `next dev` after changing.
NEXT_PUBLIC_API_URL=/api
API_PROXY_TARGET=https://logo-production-043e.up.railway.app

# Alternative: browser calls Railway directly (needs CORS on Railway).
# NEXT_PUBLIC_API_URL=https://logo-production-043e.up.railway.app/api
# CORS_ALLOWED_ORIGINS=http://localhost:3000
